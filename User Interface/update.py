import streamlit as st
import pandas as pd
from database import *

def updateAnimal():
    # result = viewAnimalData()
    # # st.write(result)
    # df = pd.DataFrame(result, columns=['Animal ID', 'Animal Name', 'Sex', 'PlaceofOrigin', 'Dead or Alive', 'Age', 'Neuterer', 'Euthanizer', 'Lives at', 'Adopted by', 'Species', 'Breed'])
    # with st.expander("View all Animals"):
    #     st.dataframe(df)
    # list_of_trains = [i[0] for i in viewAnimalID()]
    # selected_train = st.selectbox("Train to Edit", list_of_trains)
    # selected_result = getAnimal(selected_train)
    # new_availability = []
    # # st.write(selected_result)
    # if selected_result:
    #     oldAnimalID , AnimalID= selected_result[0][0]
    #     AName = selected_result[0][1]
    #     Sex = selected_result[0][2]
    #     PlaceofOrigin = selected_result[0][3]
    #     DeadorAlive = selected_result[0][4]
    #     Age = selected_result[0][5]
    #     NeuterEmployeeID = selected_result[0][6]
    #     EuthanizeEmployeeID = selected_result[0][7]
    #     LivesShelterlicense = selected_result[0][8]
    #     AdoptedSSN = selected_result[0][9]
    #     Species = selected_result[0][10]
    #     Breed = selected_result[0][11]
    #     # Layout of Create
        
        
      
    #     if st.button("Update Train"):
    #         updateAnimalData(AnimalID, AName, Sex, PlaceofOrigin, DeadorAlive, Age, NeuterEmployeeID, EuthanizeEmployeeID, LivesShelterlicense, AdoptedSSN, Species, Breed, oldAnimalID)
    #         st.success("Successfully updated availability of {} to Yes".format(train_name))
    

    # result2 = view_all_data()
    # df2 = pd.DataFrame(result, columns=['Train number', 'Train Name', 'Train Type', 'Source', 'Destination', 'Availability'])
    # with st.expander("Updated data"):
    #     st.dataframe(df2)
    return

def updateCustomer():
    return

def updateEmployee():
    return

def updateShelter():
    return

def updateGBody():
    return

def updateDonations():
    return

def updateRescues():
    return

def updateCares():
    return

def updateMonitor():
    return